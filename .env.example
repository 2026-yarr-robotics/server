# Copy to .env (gitignored) and fill in the value. Do NOT commit .env.
#
# Cloudflare Zero Trust tunnel connector token for the dashboard-managed
# tunnel. Get it from: Zero Trust -> Networks -> Tunnels -> <tunnel> ->
# Configure -> the `--token eyJ...` string.
CF_TUNNEL_TOKEN=
